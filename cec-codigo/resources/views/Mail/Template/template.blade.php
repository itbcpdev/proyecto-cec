<!DOCTYPE html>
<html lang="en">

    @yield('head')

    <body style="max-width: 700px;margin:auto">
        <div>
            @yield('header')
            @yield('content')
            @yield('footer')
        </div>
    </body>
</html>
